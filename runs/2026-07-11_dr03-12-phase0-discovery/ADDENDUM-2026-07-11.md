# Addendum (2026-07-11, Folge-Commission cloud-privacy-gate)

**Korrektur zu OP-0.1 Punkt 4 dieses Reports:** Die Aussage „Mails der Kategorien Bank/Steuer … **können** den Cloud-Call erreichen" war **unvollständig**: sie prüfte nur die lokale Kette `Paperless Gate → … → LLM-Bestätigung`, nicht deren Erreichbarkeit vom Mail-Eingang. Tatsächlich ist die gesamte Kette in v1.5 (beide Zwillinge) **strukturell unerreichbar** — `Paperless Gate IF` (candidate==true → Nextcloud Task Dokument) und der re-gatende Code-Node `Paperless Gate` (candidate===true, aber nur der false-Zweig kommt dort an) schließen sich gegenseitig aus; zudem kennt der Kategorien-Switch weder `Bank` noch `Steuer` (Normalisierung → `Review / Sonstiges`). Statisch + live (VM300, 5 Fixtures) belegt in `runs/2026-07-11_phase0-cloud-privacy-gate/`. Entspricht #21-Befund 2 (forward-branch-dead), eine Ebene höher gelesen.

Der ungegatete Cloud-Call-Node und die Konstante `data_class='S2'` bleiben als Code-Fakten korrekt; der Branch-B-Gesamtbefund der Discovery ist unverändert.
